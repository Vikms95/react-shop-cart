import React from 'react';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { act } from 'react-dom/test-utils';
import ImageSliderArrow from './ImageSliderArrow';

it('triggers function from arrow on click', () => {
//   const mockMoveSlider = jest.fn();
//   render(<ImageSliderArrow
//     moveSlider={mockMoveSlider}
//   />);

  //   const button = screen.getByTestId('arrow');

  //   act(() => {
  //     userEvent.click(button);
  //     userEvent.click(button);
  //   });

//   expect(mockMoveSlider).toHaveBeenCalledTimes(2);
});
